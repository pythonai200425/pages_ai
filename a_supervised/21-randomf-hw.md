
# תרגיל – יער רנדומלי (Random Forest)

נושא: זיהוי סוג בעל חיים (cat / dog / horse) על סמך תכונות מדידות: גובה בס"מ, משקל בק"ג, ואורך אוזן בס"מ

| גובה (ס"מ) | משקל (ק"ג) | אורך אוזן (ס"מ) | סוג |
|-----------:|------------:|-----------------:|:----:|
| 25        | 3.5         | 8               | cat  |
| 30        | 4.2         | 9               | cat  |
| 28        | 3.8         | 8.5             | cat  |
| 50        | 18.0        | 12              | dog  |
| 55        | 22.0        | 13              | dog  |
| 48        | 16.5        | 11              | dog  |
| 150       | 450.0       | 60              | horse|
| 140       | 420.0       | 58              | horse|
| 160       | 480.0       | 62              | horse|
| 52        | 20.0        | 12.5            | dog  |

1. בנו מודל `RandomForestClassifier` שממיין את סוג החיה (cat / dog / horse)
2. חלקו את הדאטה לסט אימון וסט בדיקה (train/test split — השתמשו ב-80% אימון, 20% בדיקה)
3. אימנו את המודל על סט האימון
4. חשבו את דיוק המודל (`Model Accuracy`) על סט הבדיקה
	- *רמז: השתמשו ב-`accuracy_score` מ-`sklearn.metrics`*
5. חשבו את דיוק ה־Out-of-Bag (`OOB Accuracy`) של המודל
	- *רמז: השתמשו ב-`oob_score_` מהמודל — אל תשכחו לאפשר `oob_score=True` כשאתם יוצרים את המודל*
6. בצעו חיזוי עבור חיה חדשה עם התכונות הבאות ותשיבו מה הסוג המנובא. הוסיפו את קוד החיזוי וכתבו את התוצאה:
	- גובה: 45 ס"מ
	- משקל: 12 ק"ג
	- אורך אוזן: 10 ס"מ

---

## Starter Python Code

```python
import numpy as np
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score, confusion_matrix

# Dataset: [Height(cm), Weight(kg), EarLength(cm)]
X = np.array([
	 [25, 3.5, 8],
	 [30, 4.2, 9],
	 [28, 3.8, 8.5],
	 [50, 18.0, 12],
	 [55, 22.0, 13],
	 [48, 16.5, 11],
	 [150, 450.0, 60],
	 [140, 420.0, 58],
	 [160, 480.0, 62],
	 [52, 20.0, 12.5]
])

# Labels: 'cat', 'dog', 'horse'
y = np.array(['cat', 'cat', 'cat', 'dog', 'dog', 'dog', 'horse', 'horse', 'horse', 'dog'])
```

יש לשלוח את הפתרון למייל:
📧 [pythonai200425+randf1@gmail.com](mailto:pythonai200425+randf1@gmail.com)